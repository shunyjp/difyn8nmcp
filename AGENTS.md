# AGENTS.md

## このリポジトリの目的

このリポジトリは、Dify と n8n の運用・構築を支援する MCP サーバ / API サーバを開発するためのリポジトリです。

主な目的は以下です。

- Dify のアプリ、Workflow、DSL、API 実行状態を扱いやすくする
- n8n のワークフロー、実行履歴、Webhook、連携状態を扱いやすくする
- MCP クライアントから Dify / n8n を操作できるようにする
- 将来的に FastAPI + OpenAPI により、Custom GPT Actions からも呼び出せる API サーバに拡張する
- Dify と n8n の連携診断、設定確認、よくあるエラーの原因推定を支援する

## 想定する技術スタック

- Python
- FastAPI
- MCP SDK
- Dify API
- n8n API
- pytest
- python-dotenv
- requests または httpx

## 開発時の基本方針

1. 既存ファイルを壊さないこと。
   - 既存コードを削除・大幅変更する前に、目的と影響範囲を明確にする。
   - 既存の MCP ツール、スクリプト、サンプルファイルがある場合は、後方互換性を優先する。

2. 実装よりも安全な構成整理を優先すること。
   - 仕様、README、環境変数、テスト方針を明確にしてから実装を進める。
   - 大きな変更は小さな PR に分割する。

3. 秘密情報をコミットしないこと。
   - API キー、トークン、Dify URL、n8n URL、Basic 認証情報、Webhook URL などを直接ファイルに書かない。
   - `.env` を利用し、サンプル値は `.env.example` に記載する。
   - 実際の値、個人環境の URL、アクセストークンを含むログをコミットしない。

4. 環境変数で設定すること。
   - Dify API キー、Dify Base URL、n8n API キー、n8n Base URL は環境変数で扱う。
   - コード内では `os.getenv()` または設定管理用モジュール経由で参照する。

5. エラー診断を重視すること。
   - Dify の `not_workflow_app` など、既知のエラーは原因と対処を返せるようにする。
   - API 通信エラーはステータスコード、エラー本文、想定原因を整理して返す。

6. 日本語利用者に読みやすい説明を優先すること。
   - README、docs、エラーメッセージ、PR 説明は日本語を基本とする。
   - コードコメントは必要に応じて日本語または英語で簡潔に書く。

## 環境変数の扱い

`.env` はローカル専用ファイルとして扱い、コミットしないでください。

想定する環境変数は以下です。

```env
DIFY_BASE_URL=https://example-dify.example.com
DIFY_API_KEY=replace-with-your-dify-api-key
N8N_BASE_URL=https://example-n8n.example.com
N8N_API_KEY=replace-with-your-n8n-api-key
MCP_SERVER_HOST=127.0.0.1
MCP_SERVER_PORT=8000
LOG_LEVEL=INFO
```

## PR作成時のルール

PR には以下を含めてください。

- 変更概要
- 変更対象ファイル
- 動作確認内容
- 影響範囲
- 未対応事項
- 秘密情報を含んでいないことの確認

PR は原則として以下の単位で作成します。

- ドキュメント整備
- Dify 機能追加
- n8n 機能追加
- 診断機能追加
- FastAPI / OpenAPI 対応
- テスト追加

## テスト実行方法

現時点でテスト構成が未整備の場合は、まず以下の方針で整備してください。

```bash
python -m pytest
```

API疎通確認系のテストは、実環境の API キーを必要とするため、通常の単体テストとは分離してください。

推奨方針:

- 単体テスト: モックを使い、API キー不要で実行可能にする
- 結合テスト: `.env` の設定がある場合のみ実行する
- 外部 API を呼ぶテストは、明示的なオプションなしに CI で実行しない

## README更新ルール

以下の変更を行った場合は README も更新してください。

- 起動方法の変更
- 環境変数の追加・変更
- Dify / n8n API の操作機能追加
- MCP ツール名や引数の変更
- FastAPI エンドポイントの追加・変更
- 依存ライブラリの追加
- セットアップ手順の変更

README は、このリポジトリを初めて見る利用者がローカルで動作確認できる粒度で記述してください。

## 禁止事項

- 秘密情報、API キー、トークン、実運用 URL のコミット
- 既存コードの意図しない削除
- 動作未確認の大規模リファクタリング
- README と実装が食い違う変更
- `.env` のコミット
- 個人環境に依存した絶対パスのコミット

## Codexへの期待

Codex は、実装前に既存構成を確認し、必要な範囲だけを変更してください。

不明点がある場合は、推測で大きく作り替えるのではなく、以下を優先してください。

1. 現状構成の把握
2. 小さな改善
3. ドキュメント追記
4. テスト追加
5. PRでの明確な説明
